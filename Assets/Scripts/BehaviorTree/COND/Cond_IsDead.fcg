import "StdLibrary.fcc" as StdLib
import "EditorGenLib.fcc" as EditorLib
import "BehaviourTree.fcc" as BehaviourTree

[platform_client]
//Fake Client: Currently using server script
graph FC_Cond_IsDead {
    //Executed when entity is created

    func PlayConditon(Owner entity<BevTreeAttachableForClient>) bool {
		// LogInfo("playCondition: IsDead")
		// LogInfo("Owner: " + Owner)
		// LogInfo("Owner: " + Owner<Mob>.HP)
		return (Owner<Mob>.HP <= 0)
	}
}